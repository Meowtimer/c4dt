Intern:
- c4parser
-- > neue indexierungsquelle
-- > neue markerquelle
- einbindung des parsers in einen inkrementellen build

- indexierte Daten des Clonk-Verzeichnisses per java.beans speichern. (genau wie engine.xml)
-- wo? im projekt verzeichnis? in irgendeinen workspace->plugin ordner (gibts sowas?)?

-Eclipse distro multiplatform compatible

GUI:
-  New-Project-Wizard
-- fehlende Texte ergänzen
-- fehlende Optionsfelder erg�änzen (wenn welche fehlen)

- New-Object-Wizard
-- Namen in die entsprechenden Dateien einfließen lassen.
-- Vielleicht schon nach deutsch/englisch Übersetzung nachfragen.

+ New-Objectcontainer-Wizard
+ New-Scenario-Wizard
+ New-*-Wizard ... blablabla

+ Konfigurationseditor (DefCore, ActMap etc.)
+ Möglichkeit, Szenario per Engine zu testen (Run Command)

-  C4ScriptEditor
-- sollte auch Skripte aus System.c4g öffnen (szenarienlokale Zusatzskripte zB)